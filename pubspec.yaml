name: reminder_reverpod_app
description: "A new Flutter project."
publish_to: "none"

version: 1.0.0+1

environment:
  sdk: ">=3.3.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.6
  flutter_local_notifications: ^17.0.0
  rxdart: ^0.27.7
  timezone: ^0.9.2

  hive: ^2.2.3
  hive_flutter: ^1.1.0

  intl: ^0.19.0

  flutter_riverpod: ^2.4.10

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

  build_runner:
  hive_generator: ^2.0.1

flutter:
  uses-material-design: true
